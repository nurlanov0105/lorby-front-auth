import { LoginForm } from '@/features/auth';
import styles from './styles.module.scss';

const Login = () => {
   return (
      <>
         <LoginForm />
      </>
   );
};

export default Login;
